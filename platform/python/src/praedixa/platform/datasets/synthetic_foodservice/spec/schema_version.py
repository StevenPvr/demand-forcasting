"""Versioning for the restaurant simulator relational schema and spec assets."""

from __future__ import annotations

# Bump when any column, FK, or enum in data_dictionary.yaml changes materially.
SCHEMA_VERSION: str = "1.0.0"
